import { FC } from 'react';
import Styled from './Burder.styles';

const Burger: FC = () => {
  return (
    <Styled.Wrapper>
      <Styled.Item />
      <Styled.Item />
      <Styled.Item />
    </Styled.Wrapper>
  );
};

export default Burger;
